---
# Default CLI Text Editor
editor_env_arcfg_name: "{{ default_cli_text_editor }}"

# Default VISUAL Text Editor
default_visual_text_editor: "{{ default_visual_text_editor }}"

# Default terminal emulator
default_terminal_emulator: "{{ default_terminal_emulator }}"
